{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "nixpacks",
        "buildCommand": "yarn --cwd client install"
    },
    "deploy": {
        "startCommand": "yarn start",
        "healthcheckPath": "/api/serverhealth",
        "healthcheckTimeout": 100,
        "restartPolicyType": "never"
    }
}
